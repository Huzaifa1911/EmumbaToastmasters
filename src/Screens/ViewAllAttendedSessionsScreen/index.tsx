import React from 'react';

import {ScreenWrapper} from 'Components';
import {AttendedSessionList} from './styles';

const ViewAllAttendedSessions = () => {
  return (
    <ScreenWrapper>
      <AttendedSessionList />
    </ScreenWrapper>
  );
};

export default ViewAllAttendedSessions;
