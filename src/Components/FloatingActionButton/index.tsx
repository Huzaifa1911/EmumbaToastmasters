import React from 'react';
import {FABProps} from 'react-native-paper';

import {FabActionButton} from './styles';

const FloatingActionButton = (props: FABProps) => {
  return <FabActionButton {...props} />;
};

export default FloatingActionButton;
